name: pua-bot
services:
  pua-bot:
    build: .
    ports:
      - "8787:8787"
    volumes:
      - ./config.json:/app/config.json:ro
      - ./wrangler.toml:/app/wrangler.toml:ro
      - ./data:/data
    environment:
      SCRIPT_FILE_PATH: /data/scripts.md
    network_mode: "host"
