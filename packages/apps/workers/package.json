{
    "name": "@pua-bot/workers",
    "type": "module",
    "version": "0.1.0",
    "private": true,
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "scripts": {
        "build": "pnpm vite build",
        "clean": "rm -rf dist && rm -rf node_modules && rm -rf tsconfig.tsbuildinfo",
        "deploy": "wrangler deploy --config ${TOML_PATH}"
    },
    "dependencies": {
        "@pua-bot/core": "workspace:*"
    },
    "devDependencies": {
    }
}
